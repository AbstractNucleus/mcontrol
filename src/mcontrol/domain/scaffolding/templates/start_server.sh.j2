#!/usr/bin/env bash
set -euo pipefail
{{ rcon_prelude }}{% if custom_start_script %}exec bash {{ custom_start_script }}{% else %}exec java -Xmx{{ xmx_gb }}g {{ jvm_extra_args }} -jar "{{ server_jar }}" nogui{% endif %}
